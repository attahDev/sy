import ComingSoon from "@/components/dashboard/ComingSoon";

export default function AcademyPage() {
  return (
    <ComingSoon
      title="Academy"
      description="The education hub from GMBT — course catalog, lessons, quizzes, and mock exams — will be ported here next. It uses the same /courses?category=education API."
    />
  );
}
